function setCurrentPage(pageName: string) {
    console.log(pageName);
    // console.log('window.myCurrentPage before', window.myCurrentPage);
    // window.myCurrentPage = pageName;
    // console.log('window.myCurrentPage after', window.myCurrentPage);
};
